# Object files
*.o

# macOS Executables (compiled binaries with no extension)
# Note: This matches the project root. If your executable is in a subfolder, 
# you can use a wildcard or target the specific name (e.g., /my_program).
*
!*/
!*.*
!.gitignore
!.git
